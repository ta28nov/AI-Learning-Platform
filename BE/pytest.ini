[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: full student flow tests (slower)
    ai: tests relying on mocked AI responses
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
